
kind: blueprint-profile
name: post_build
environment-type: sandbox
cloud: kube_test
services:
    - dev-box

---

kind: blueprint-profile
name: integration
environment-type: sandbox
cloud: aws
services:
    - dev-box

