post_build_tests:
    kind: blueprint-profile
    environment-type: sandbox
    cloud: kube_test
    services:
        - dev-box

----

integration_tests:
    kind: blueprint-profile
    environment-type: sandbox
    cloud: aws
    services:
        - dev-box

